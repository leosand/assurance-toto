{
  "agent_name": "orchestrateur",
  "role": "Orchestrateur central — coordonne les agents départementaux",
  "model": "${OLLAMA_MODEL}",
  "ollama_host": "${OLLAMA_HOST}",
  "approval_mode": "${HERMES_APPROVAL_MODE}",
  "skills_dir": "/home/agent/workspace/skills",
  "mcp_servers": {
    "postgres": { "url": "http://mcp-postgres:8080" },
    "git": { "url": "http://mcp-git:8080" }
  },
  "tools_allowlist": "/home/agent/mcp-allowlist.json",
  "logging": {
    "level": "info",
    "format": "json"
  }
}
